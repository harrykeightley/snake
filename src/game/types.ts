export type Position = {x: number; y: number};
export type Direction = 'UP' | 'DOWN' | 'LEFT' | 'RIGHT';

export enum Keys {
  LENGTH = 'length',
  HEAD = 'head',
  DIRECTION = 'direction',
  POSITION = 'position',
  BODY = 'body',
  BOUNDS = 'bounds',
  TIMER = 'timer',
  FOOD = 'food',
}
